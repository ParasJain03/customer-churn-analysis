# Optional GenAI configuration for app.py
# Never commit your real API key.
OPENAI_API_KEY=
# Optional OpenAI-compatible endpoint, e.g. an approved provider endpoint.
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini
